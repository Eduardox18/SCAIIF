<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Aviso">
    <insert id="guardarAviso" parameterType="modelo.pojos.Aviso">
        INSERT INTO aviso (asunto, mensaje, fechaCreacion, fechaLimite) VALUES 
        (#{Asunto}, #{Mensaje}, #{fechaCreación}, #{fechaLimite});
    </insert>
</mapper>