<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Curso">

    <select id="getCursos" parameterType="String" resultType="modelo.pojos.Curso">
        SELECT nombreCurso FROM curso INNER JOIN calificacion ON
        curso.nrc = calificacion.nrc WHERE calificacion.matricula = #{matricula};
    </select>
</mapper>